# IMPORTANT: Before running docker-compose, ensure Colima is running:
# colima start
#
# This exposes the Docker socket at ~/.colima/default/docker.sock
# which is then accessible inside containers at /var/run/docker.sock


